# Backend API base URL (no trailing slash).
# - Local dev: frontend on :8080, backend on :8000
VITE_API_URL=http://localhost:8000
# - Lovable deploy: In Lovable project env, set VITE_API_URL to your Railway backend URL
#   (e.g. https://aura-briefing-production.up.railway.app) or leave unset to use that default.
