# Backend API URL (used at build time).
# - Local dev: use .env.development with VITE_API_URL=http://localhost:8000
# - Production (frontend on different host): set to your Railway backend URL, e.g.:
VITE_API_URL=https://aura-briefing-production.up.railway.app
