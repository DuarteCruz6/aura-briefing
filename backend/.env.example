# Required for STT / TTS (ElevenLabs). Get a key at https://elevenlabs.io
ELEVENLABS_API_KEY=

# Required for LLM (summaries). Get a key at https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Optional: YouTube Data API v3 for video metadata (title, description, channel). Transcript uses youtube-transcript-api (no key).
# Get a key at https://console.cloud.google.com/apis/credentials (enable YouTube Data API v3).
# YOUTUBE_API_KEY=

# Optional: Apify API token for X (Twitter) search and LinkedIn. Get at https://console.apify.com/account/integrations
# When set, /feed/x-by-topics uses Apify (scraper_one/x-posts-search) instead of Nitter.
# APIFY_API_TOKEN=

# Optional: Nitter instance for X/Twitter (fallback when APIFY_API_TOKEN is not set). Default https://nitter.net
# NITTER_BASE_URL=https://nitter.net

# Optional (local)
# ENVIRONMENT=development
# DATABASE_URL=sqlite:///./data/newsletter.db

# Railway (backend): set in dashboard Variables.
# For a persistent DB: add a PostgreSQL service in the same project, then set
# DATABASE_URL to a reference to that service's DATABASE_URL variable.
# CORS: frontend origin(s), or "*" to allow any origin (avoids OPTIONS 400 when frontend URL varies).
# CORS_ORIGINS=https://your-app.lovable.app,http://localhost:5173
# CORS_ORIGINS=*
# PORT is set by Railway automatically.